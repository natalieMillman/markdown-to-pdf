version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run test:ci
        - npm run build
  artifacts:
    baseDirectory: dist/markdown-to-pdf
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
